model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Trace code, config, and dependencies carefully.
Cite concrete local evidence.
Do not jump into implementation unless explicitly asked.
"""
