# AgentCodex Runtime Bundle

This directory is populated by the runtime bundle build step.

Expected output path:

- `plugins/agentcodex/runtime/agentcodex-runtime/`

That built bundle is the payload the Codex plugin can ship without requiring users to clone the AgentCodex source repository.
